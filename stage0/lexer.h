#ifndef KAI_LEXER_H
#define KAI_LEXER_H

#include <stddef.h>

/*
 * Milestone 1 stub: acknowledges the source buffer and exits.
 * Milestone 2 replaces this with a real tokenizer producing a token
 * stream for the parser.
 */
void kai_lex(const char *file, const char *src, size_t len);

#endif /* KAI_LEXER_H */
